<%= form_for :comment, url: comments_path  do |f|%>
  <%= f.hidden_field :post_id, value: post.id  %>
  <%= f.label :content %>
  <%= f.text_area (:content) %>

  <div>
  <%= f.submit 'send comment' %>
  </div>

<% end %>