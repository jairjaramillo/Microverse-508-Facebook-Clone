<%= form_for :friendship, url: friendships_path do |f| %>
<%= f.hidden_field :sender_id, value: current_user.id %>
<%= f.hidden_field :receiver_id, value: user.id %>
<%= f.submit 'Friend User' %>
<% end %>